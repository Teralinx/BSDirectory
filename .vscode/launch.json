{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB Debug",
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${config:build_root}/BSDirectory${config:suffix}",
            "args": [],
            "cwd": "${config:build_root}",
            "internalConsoleOptions": "neverOpen",
            "env": 
                {
                    "PATH": "${config:compiler_chain_bin};${config:qt_bin};${env:PATH}"
                }            
        }
    ]
}